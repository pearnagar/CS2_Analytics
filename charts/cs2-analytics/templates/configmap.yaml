apiVersion: v1
kind: ConfigMap
metadata:
  name: cs2-config
data:
  POSTGRES_DB: {{ .Values.postgres.db | quote }}
  POSTGRES_USER: {{ .Values.postgres.user | quote }}
  MARKET_ITEMS: {{ .Values.scraper.marketItems | join "," | quote }}
